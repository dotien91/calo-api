export enum CouponPaymentMethod {
  ALL = "all",
  TRANSFER = "transfer",
  VN_PAY = "vn_pay",
}

export enum CouponType {
  FREESHIP = "freeship",
  PRODUCT = "product", // [course, book]
}

export enum CouponPromotionType {
  PERCENTAGE = "percentage",
  VALUE = "value",
}

export enum CouponVisible {
  PRIVATE = "private",
  PUBLIC = "public",
  PRODUCT = "product",
}

