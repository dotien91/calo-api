export enum OrderPaymentMethod {
  VNPAY = "vn_pay",
  TRANSFER = "transfer",
}

export enum OrderStatus {
  PENDING = "pending",
  SUCCESS = "success",
}

export enum PayloadType {
  CLASS = "class",
  ONE_ONE = "oneone",
}
